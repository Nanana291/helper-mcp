# [TASK LOCK] helper-mcp 0.6.0

- `classification`: stabilization release
- `primary focus`: reduce false positives and harden recovery/persistence
- `owned domains`: Luau analysis, brain hygiene, workspace recovery, release proof
- `must stay stable`: canonical tool names, underscore aliases, MCP resources, current hotfix snapshot contract
- `notes`: keep pattern overrides fail-closed and keep merge operations explicit

