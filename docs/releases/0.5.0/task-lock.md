# [TASK LOCK] helper-mcp 0.5.0

- `profile`: `helper-mcp`
- `taskType`: `feature-addition`
- `changeClassification`: `logic`, `brain`, `config`, `workspace`, `regression-risk`
- `selectedRoute`: expand the MCP surface while preserving current aliases and workspace-local storage
- `orderedAgents`: `core-dispatch -> luau-analysis -> workspace-baseline -> brain-import -> config-validation -> regression-proof`
- `requiredSkills`: `brainstorming`, `delivery-gating`, `artifact-enforcement`, `verification-before-completion`
- `requiredArtifacts`: `task-lock`, `handoff`, `delivery-gate`, `regression-matrix`
- `stopLineRisks`: breaking existing tool aliases, unsafe automated Luau edits, missing baseline coverage, config schema false positives
