# [TASK LOCK] helper-mcp 0.6.2

- `classification`: memory bridge release
- `primary focus`: Luau findings auto-write into durable brain notes
- `owned domains`: normalized Luau findings, deterministic bridge identity, finding history/graph/prune, release proof
- `must stay stable`: canonical tool names, underscore aliases, existing 0.6.1 commands, fail-closed pattern overrides
- `notes`: one finding maps to one brain note, and repeated scans refresh the same note instead of duplicating it
