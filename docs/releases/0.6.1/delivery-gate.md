# [DELIVERY GATE] helper-mcp 0.6.1

- `baselineUpdated`: yes, via regression tests around Luau intelligence, brain graph recovery, workspace restores, and MCP surface count
- `regressionMatrix`: yes, documented in `docs/releases/0.6.1/regression-matrix.md`
- `oldToNewMapping`: stable surface; 0.6.0 behavior remains intact and `brain.diff` was added to close the expected canonical surface count
- `ownedDomainsClosed`: Luau intelligence, brain platform, workspace automation, release proof
- `openRisks`: heuristic commands still rely on deterministic text analysis, so future scope increases should be covered by fixture tests before expansion
- `blockedOrReady`: ready after fresh suite verification and push verification
