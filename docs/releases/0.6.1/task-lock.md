# [TASK LOCK] helper-mcp 0.6.1

- `classification`: expansion release
- `primary focus`: deeper Luau intelligence, richer brain relationships, and workspace release automation
- `owned domains`: Luau graphs and scoring, brain graph and recovery, workspace diffs and restores, MCP surface proof
- `must stay stable`: canonical tool names, underscore aliases, recovery artifact paths, fail-closed pattern overrides, 0.6.0 compatibility behavior
- `notes`: keep confidence metadata attached to heuristic findings and keep restore/rollback paths deterministic
